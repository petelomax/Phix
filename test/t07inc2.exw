--
-- t07inc2.exw
--
-- Phix tests for include file handling.
--
without warning -- lots of unused stuff in here

include t07inc2a.e -- any of a,b,c,d should work.
include t07inc2b.e -- should have no effect
include t07inc2c.e -- should have no effect
include t07inc2d.e -- should have no effect
--if getc(0) then end if
